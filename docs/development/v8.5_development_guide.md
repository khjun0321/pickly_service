# 🛠️ Pickly v8.5 개발 가이드라인

> **대상**: Claude Code Agent, 개발자
> **목적**: PRD v8.5 Master Final 기준 개발 규칙 정의
> **최종 수정**: 2025-10-31

---

## 🚫 절대 규칙 (NEVER DO)

### 1. Flutter UI 변경 금지

```bash
# ❌ 절대 변경 금지
/apps/pickly_mobile/lib/features/**

# 예외 없음. 어떤 이유로도 수정 불가:
# - 버그 수정도 Repository Layer에서만
# - 디자인 변경은 v9.0 이후
# - 새 화면 추가도 금지
```

**위반 시 조치**:
- PR 자동 거부
- 변경사항 즉시 Revert
- 책임자 경고

### 2. Design System 변경 금지

```bash
# ❌ 절대 변경 금지
/packages/pickly_design_system/**

# 허용: extends만 가능
# - 새 컴포넌트는 별도 패키지로
# - 색상/폰트/간격 토큰 수정 금지
```

### 3. 기존 Migration 수정 금지

```bash
# ❌ 수정 금지
/backend/supabase/migrations/2025*.sql

# ✅ 새 마이그레이션 생성
/backend/supabase/migrations/20251031000001_new_feature.sql
```

---

## ✅ 작업 가능 영역

### 1. Flutter Repository Layer

```bash
# ✅ 변경 가능
/apps/pickly_mobile/lib/contexts/**

# 허용 작업:
- Repository 로직 수정
- Provider 추가/수정
- Model Freezed 클래스
- Exception 처리
- Network 호출
```

**개발 패턴**:

```dart
// ✅ 올바른 Repository 패턴
@riverpod
class BenefitRepository {
    final SupabaseClient _client;

    // Stream 기반 Realtime 데이터
    Stream<List<Announcement>> watchAnnouncements() {
        return _client
            .from('announcements')
            .stream(primaryKey: ['id'])
            .order('created_at', ascending: false)
            .map((data) => data.map(Announcement.fromJson).toList());
    }

    // Future 기반 일회성 데이터
    Future<Announcement> getAnnouncement(String id) async {
        final response = await _client
            .from('announcements')
            .select()
            .eq('id', id)
            .single();
        return Announcement.fromJson(response);
    }

    // 에러 처리 필수
    try {
        // ...
    } on PostgrestException catch (e) {
        throw AnnouncementNetworkException(e.message);
    } catch (e, stackTrace) {
        throw AnnouncementException(e.toString(), stackTrace);
    }
}
```

### 2. Admin React App

```bash
# ✅ 자유롭게 변경 가능
/apps/pickly_admin/src/**

# 단, Material UI 기준만 준수:
- MUI v5 컴포넌트만 사용
- Tailwind/Antd/Shadcn 금지
- Pickly DS 색상 토큰 사용
```

**개발 패턴**:

```tsx
// ✅ 올바른 Admin 컴포넌트
import { Box, Button, DataGrid } from '@mui/material';
import { useQuery, useMutation } from '@tanstack/react-query';
import { supabase } from '@/lib/supabase';

export function AnnouncementManager() {
    // React Query로 상태 관리
    const { data, isLoading } = useQuery({
        queryKey: ['announcements'],
        queryFn: async () => {
            const { data } = await supabase
                .from('announcements')
                .select('*')
                .order('created_at', { ascending: false });
            return data;
        }
    });

    const updateMutation = useMutation({
        mutationFn: async ({ id, updates }) => {
            const { data } = await supabase
                .from('announcements')
                .update(updates)
                .eq('id', id)
                .select()
                .single();
            return data;
        },
        onSuccess: () => {
            queryClient.invalidateQueries({ queryKey: ['announcements'] });
            toast.success('수정되었습니다');
        }
    });

    return (
        <Box>
            <DataGrid
                rows={data || []}
                columns={columns}
                onCellEditCommit={(params) => {
                    updateMutation.mutate({
                        id: params.id,
                        updates: { [params.field]: params.value }
                    });
                }}
            />
        </Box>
    );
}
```

### 3. Supabase Backend

```bash
# ✅ 자유롭게 확장 가능
/backend/supabase/**

# 허용 작업:
- 새 마이그레이션 추가
- Edge Function 생성
- RPC 함수 작성
- RLS 정책 수정
- Trigger/Index 추가
```

**개발 패턴**:

```sql
-- ✅ 올바른 마이그레이션 (20251031000001_add_bookmark.sql)
-- Migration: Add Bookmark Feature
-- Version: v8.5.1
-- Date: 2025-10-31

-- 1. Create table
CREATE TABLE user_bookmarks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    announcement_id UUID NOT NULL REFERENCES announcements(id) ON DELETE CASCADE,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(user_id, announcement_id)
);

-- 2. Create index
CREATE INDEX idx_user_bookmarks_user_id ON user_bookmarks(user_id);
CREATE INDEX idx_user_bookmarks_announcement_id ON user_bookmarks(announcement_id);

-- 3. Enable RLS
ALTER TABLE user_bookmarks ENABLE ROW LEVEL SECURITY;

-- 4. Create policy
CREATE POLICY "Users can manage their own bookmarks"
    ON user_bookmarks
    FOR ALL
    USING (auth.uid() = user_id);

-- 5. Add comment
COMMENT ON TABLE user_bookmarks IS 'User announcement bookmarks - v8.5.1';
```

---

## 📂 디렉토리 구조 규칙

### Flutter Mobile

```
apps/pickly_mobile/lib/
├── core/                    # ❌ 변경 금지 (라우팅/테마/유틸)
├── features/                # ❌ 절대 변경 금지 (UI)
│   ├── benefit/
│   ├── benefits/
│   ├── home/
│   └── ...
└── contexts/                # ✅ 변경 가능 (데이터)
    ├── benefit/
    │   ├── models/          # Freezed 모델
    │   ├── repositories/    # Supabase 연동
    │   ├── providers/       # Riverpod Provider
    │   └── exceptions/      # 커스텀 에러
    ├── housing/
    ├── user/
    └── auth/
```

**신규 컨텍스트 추가 시**:

```dart
// 1. 모델 생성 (contexts/my_feature/models/my_model.dart)
import 'package:freezed_annotation/freezed_annotation.dart';

part 'my_model.freezed.dart';
part 'my_model.g.dart';

@freezed
class MyModel with _$MyModel {
    const factory MyModel({
        required String id,
        required String title,
        @Default(false) bool isActive,
    }) = _MyModel;

    factory MyModel.fromJson(Map<String, dynamic> json) =>
        _$MyModelFromJson(json);
}

// 2. Repository 생성 (contexts/my_feature/repositories/my_repository.dart)
class MyRepository {
    final SupabaseClient _client;
    const MyRepository(this._client);

    Stream<List<MyModel>> watchAll() {
        return _client
            .from('my_table')
            .stream(primaryKey: ['id'])
            .map((data) => data.map(MyModel.fromJson).toList());
    }
}

// 3. Provider 생성 (contexts/my_feature/repositories/my_repository.dart)
@riverpod
MyRepository myRepository(MyRepositoryRef ref) {
    return MyRepository(Supabase.instance.client);
}

@riverpod
Stream<List<MyModel>> myModels(MyModelsRef ref) {
    return ref.watch(myRepositoryProvider).watchAll();
}

// 4. 코드 생성
// flutter pub run build_runner build --delete-conflicting-outputs
```

### Admin React

```
apps/pickly_admin/src/
├── api/                     # ✅ Supabase API 호출
│   ├── announcements.ts
│   ├── banners.ts
│   └── categories.ts
├── components/              # ✅ MUI 컴포넌트
│   ├── layout/
│   ├── shared/
│   └── benefits/
├── pages/                   # ✅ 화면별 페이지
│   ├── benefits/
│   ├── banners/
│   └── dashboard/
├── hooks/                   # ✅ 커스텀 훅
├── types/                   # ✅ TypeScript 타입
└── lib/                     # ✅ 유틸/설정
    └── supabase.ts
```

**신규 페이지 추가 시**:

```tsx
// 1. API 함수 생성 (src/api/my_feature.ts)
export async function fetchMyItems() {
    const { data, error } = await supabase
        .from('my_table')
        .select('*')
        .order('created_at', { ascending: false });

    if (error) throw error;
    return data;
}

// 2. 페이지 컴포넌트 (src/pages/my-feature/MyFeaturePage.tsx)
export function MyFeaturePage() {
    const { data, isLoading } = useQuery({
        queryKey: ['my-items'],
        queryFn: fetchMyItems
    });

    if (isLoading) return <CircularProgress />;

    return (
        <Box>
            <Typography variant="h4">My Feature</Typography>
            <DataGrid rows={data} columns={columns} />
        </Box>
    );
}

// 3. 라우트 추가 (src/App.tsx)
<Route path="/my-feature" element={<MyFeaturePage />} />
```

### Supabase Backend

```
backend/supabase/
├── migrations/              # ✅ 새 파일만 추가
│   └── YYYYMMDDHHMMSS_description.sql
├── functions/               # ✅ Edge Functions
│   ├── fetch-lh-announcements/
│   ├── fetch-sh-announcements/     # 추가 필요
│   └── transform-announcements/    # 추가 필요
└── seed.sql                 # ✅ 초기 데이터
```

**Edge Function 추가 시**:

```typescript
// functions/my-function/index.ts
import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
import { createClient } from '@supabase/supabase-js';

serve(async (req) => {
    try {
        const supabase = createClient(
            Deno.env.get('SUPABASE_URL')!,
            Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
        );

        // 비즈니스 로직
        const { data, error } = await supabase
            .from('my_table')
            .select('*');

        if (error) throw error;

        return new Response(
            JSON.stringify({ success: true, data }),
            { headers: { 'Content-Type': 'application/json' } }
        );
    } catch (error) {
        return new Response(
            JSON.stringify({ success: false, error: error.message }),
            { status: 500, headers: { 'Content-Type': 'application/json' } }
        );
    }
});
```

---

## 🔄 개발 워크플로우

### 1. 새 기능 개발 프로세스

```bash
# Step 1: 브랜치 생성
git checkout -b feature/my-feature-name

# Step 2: 우선순위 결정
# 1순위: Supabase 스키마 (필요시)
# 2순위: Admin UI (관리 기능)
# 3순위: Flutter Repository (데이터 레이어)
# 4순위: 테스트 및 검증

# Step 3: Supabase 마이그레이션
cd backend/supabase/migrations
cat > 20251031000001_my_feature.sql << EOF
-- Migration: My Feature
-- Version: v8.5.1
CREATE TABLE my_table (...);
EOF

# Step 4: Admin UI 개발
cd apps/pickly_admin/src
# - api/my_feature.ts 생성
# - pages/my-feature/MyFeaturePage.tsx 생성

# Step 5: Flutter Repository 개발
cd apps/pickly_mobile/lib/contexts/my_feature
# - models/ 생성
# - repositories/ 생성
# - providers/ 생성

# Step 6: 테스트
# - Supabase Local 테스트
# - Admin UI 기능 테스트
# - Flutter Repository 단위 테스트

# Step 7: PR 생성
git add .
git commit -m "feat: add my feature for v8.5"
git push origin feature/my-feature-name
```

### 2. 버그 수정 프로세스

```bash
# Step 1: 문제 위치 파악
# - Flutter UI 버그? → Repository에서 수정
# - Admin 버그? → 컴포넌트 수정
# - 데이터 버그? → Supabase 수정

# Step 2: 브랜치 생성
git checkout -b fix/bug-description

# Step 3: Repository Layer에서 수정 (Flutter UI 건드리지 말 것!)
# ❌ 잘못된 방법
# features/benefit/screens/announcement_detail_screen.dart 수정

# ✅ 올바른 방법
# contexts/benefit/repositories/benefit_repository.dart 수정

# Step 4: 테스트 및 PR
```

### 3. 데이터 마이그레이션

```bash
# ❌ 절대 하지 마세요
# - 기존 마이그레이션 파일 수정
# - DROP TABLE 실행
# - 데이터 손실 가능한 ALTER

# ✅ 올바른 방법
# 1. 새 마이그레이션 파일 생성
cat > migrations/20251031000001_add_column.sql << EOF
-- 기존 데이터 보존하며 컬럼 추가
ALTER TABLE announcements
ADD COLUMN IF NOT EXISTS new_field TEXT;

-- 기본값 설정
UPDATE announcements
SET new_field = 'default_value'
WHERE new_field IS NULL;

-- NOT NULL 제약조건 (선택사항)
ALTER TABLE announcements
ALTER COLUMN new_field SET NOT NULL;
EOF

# 2. 테스트 (로컬 Supabase)
supabase db reset
supabase db push

# 3. 검증
supabase db diff
```

---

## 🧪 테스트 가이드라인

### Flutter Repository 테스트

```dart
// test/contexts/benefit/repositories/benefit_repository_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:mockito/mockito.dart';

void main() {
    late BenefitRepository repository;
    late MockSupabaseClient mockClient;

    setUp(() {
        mockClient = MockSupabaseClient();
        repository = BenefitRepository(mockClient);
    });

    group('getAnnouncements', () {
        test('should return list of announcements', () async {
            // Arrange
            when(mockClient.from('announcements').select())
                .thenReturn(mockResponse);

            // Act
            final result = await repository.getAnnouncements();

            // Assert
            expect(result, isA<List<Announcement>>());
            expect(result.length, 3);
        });

        test('should throw AnnouncementException on error', () async {
            // Arrange
            when(mockClient.from('announcements').select())
                .thenThrow(PostgrestException(message: 'Error'));

            // Act & Assert
            expect(
                () => repository.getAnnouncements(),
                throwsA(isA<AnnouncementNetworkException>())
            );
        });
    });
}
```

### Admin UI 테스트

```tsx
// src/pages/benefits/__tests__/BenefitCategoryPage.test.tsx
import { render, screen, waitFor } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { BenefitCategoryPage } from '../BenefitCategoryPage';

describe('BenefitCategoryPage', () => {
    it('should display announcements', async () => {
        const queryClient = new QueryClient();

        render(
            <QueryClientProvider client={queryClient}>
                <BenefitCategoryPage />
            </QueryClientProvider>
        );

        await waitFor(() => {
            expect(screen.getByText('주거 관리')).toBeInTheDocument();
        });
    });
});
```

---

## 📝 커밋 메시지 규칙

```bash
# 형식
<type>(<scope>): <subject>

# 타입
feat:     새 기능
fix:      버그 수정
docs:     문서 수정
style:    코드 포맷팅
refactor: 리팩토링
test:     테스트 추가
chore:    빌드/설정 변경

# 예시
feat(admin): add D-day calculation UI
fix(repository): handle null deadline_date
docs(prd): update v8.5 compliance checklist
refactor(benefit): merge benefit and benefits folders
```

---

## 🚨 자주 하는 실수

### ❌ 하지 마세요

```dart
// ❌ Flutter UI 파일 수정
// features/benefit/screens/announcement_detail_screen.dart
class AnnouncementDetailScreen extends StatelessWidget {
    Widget build(BuildContext context) {
        // 버그 수정이라도 여기서 하지 마세요!
    }
}

// ❌ Provider에서 직접 Supabase 호출
@riverpod
Stream<List<Announcement>> announcements(AnnouncementsRef ref) {
    return Supabase.instance.client  // ❌ 직접 호출 금지
        .from('announcements')
        .stream(primaryKey: ['id']);
}

// ❌ Admin에서 Tailwind 사용
<div className="flex items-center justify-between">  // ❌ Tailwind 금지
```

### ✅ 이렇게 하세요

```dart
// ✅ Repository에서 수정
// contexts/benefit/repositories/benefit_repository.dart
class BenefitRepository {
    Future<Announcement> getAnnouncement(String id) async {
        // 버그 수정은 여기서!
        final response = await _client
            .from('announcements')
            .select()
            .eq('id', id)
            .single();

        // 데이터 변환 로직 추가
        return Announcement.fromJson(response);
    }
}

// ✅ Provider는 Repository 호출만
@riverpod
Stream<List<Announcement>> announcements(AnnouncementsRef ref) {
    final repository = ref.watch(benefitRepositoryProvider);
    return repository.watchAnnouncements();  // ✅ Repository 위임
}

// ✅ Admin은 MUI만 사용
<Box sx={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
```

---

## 🔍 코드 리뷰 체크리스트

### Flutter Repository
- [ ] `features/` 폴더 수정 안 함
- [ ] Freezed 모델 사용
- [ ] Exception 처리 완료
- [ ] Stream vs Future 적절히 선택
- [ ] Provider 분리 (Repository, Data)

### Admin UI
- [ ] Material UI 컴포넌트만 사용
- [ ] React Query로 상태 관리
- [ ] Loading/Error 상태 처리
- [ ] Toast 알림 추가
- [ ] TypeScript 타입 정의

### Supabase Backend
- [ ] 새 마이그레이션 파일 생성
- [ ] 기존 마이그레이션 수정 안 함
- [ ] RLS 정책 설정
- [ ] 인덱스 최적화
- [ ] Comment 문서화

---

## 📚 참고 자료

- [PRD v8.5 Master Final](/docs/prd/PRD_v8.5_Master_Final.md)
- [v8.5 구조 분석 보고서](/docs/development/v8.5_structure_analysis.md)
- [Supabase 공식 문서](https://supabase.com/docs)
- [Riverpod 공식 문서](https://riverpod.dev)
- [Material UI 공식 문서](https://mui.com)

---

**최종 수정**: 2025-10-31
**작성자**: Claude Code Agent
**버전**: v8.5 Development Guide v1.0
