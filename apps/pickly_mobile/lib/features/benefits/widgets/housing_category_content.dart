import 'package:flutter/material.dart';
import 'package:pickly_design_system/pickly_design_system.dart';

/// Housing category content (주거 카테고리 컨텐츠)
///
/// Displays housing-related policies and benefits with dynamic banners
class HousingCategoryContent extends StatelessWidget {
  const HousingCategoryContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        // Housing benefits list - temporarily removed for redesign
        // Content will be added here later
      ],
    );
  }
}
